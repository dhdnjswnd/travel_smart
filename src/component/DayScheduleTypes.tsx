type DayScheduleType = {
  day: string;
  date: string;
  weather: string;
};

type DayScheduleDetailType = {
  spot: string;
  time: string;
  type: string;
};

export type { DayScheduleDetailType, DayScheduleType };
